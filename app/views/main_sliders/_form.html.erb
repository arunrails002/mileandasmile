<%= form_for(main_slider) do |f| %>
  <% if main_slider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main_slider.errors.count, "error") %> prohibited this main_slider from being saved:</h2>

      <ul>
      <% main_slider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
