<%= form_for(mile_service) do |f| %>
  <% if mile_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mile_service.errors.count, "error") %> prohibited this mile_service from being saved:</h2>

      <ul>
      <% mile_service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
